    """# Strings

Four zyBooks sections (3.1-3.3, 3.5) on how Python represents, slices,
formats, and searches text -- rebuilt as runnable CodeSlides cells.
Every code sample lives in its own **test editor** below the notes, not
the slide's main editor -- run it, change it, break it.

Use **Slides** to step through in order, or **Cells** to jump straight
to a topic."""


    """## Strings Are a Sequence of Characters

A **string** is a sequence of characters that represents textual data,
like a location or a message to the user. A **string literal** is a
string defined directly in source code by surrounding a value with
single or double quotes, like `'MARY'` or `"MARY"`.

A string is a **sequence type** - a type that orders a collection of
objects (here, characters) into a sequence, each with its own numbered
**index** starting at `0`."""
    indices = "  ".join(str(i) for i in range(len(sample_word)))
    letters = "  ".join(sample_word)
    summary = f"index:  {indices}\nchar:   {letters}"
    print(f"text\n{summary}\n")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")
    # indices = "  ".join(str(i) for i in range(len(sample_word)))
    # letters = "  ".join(sample_word)
    # summary = f"index:  {indices}\nchar:   {letters}"
    # return cs.md(f"```text\n{summary}\n```")


def string_length():
    """## Finding a String's Length

The **`len()`** built-in function returns the length of any sequence
object passed as an argument (a string, or another sequence type like a
list).

```python
gandhi = "Mohandas Karamchand Gandhi"
print(len(gandhi))   # 26
```

Manually counting characters in a long string literal is error-prone --
`len()` counts them exactly, every time. Run the test to compare the
lengths of three very differently sized names."""


def string_indexing():
    """## IndexingPositive and negative indexing

A programmer can reference a single character at a specific index by
appending square brackets containing the desired index after a
string's name. `alphabet[0]` is the first character, `"A"`;
`alphabet[19]` is the 20th character, `"T"`.

A **negative index** counts backward from the end of the string, which
is useful when a string is very long or its length isn't known in
advance -- `alphabet[-1]` is the *last* character, and `alphabet[-26]`
is the first character of a 26-character string (the same as
`alphabet[0]`).

Run the test and match each printed character back to its index."""


def slicing_basics(slice_source, slice_start, slice_end):
    """TR8am finished here
## Slicing

**Slice notation** has the form `my_str[start:end]`, which creates a
*new* string containing the characters of `my_str` from index `start`
up to, but not including, index `end`.

```python
url = "http://en.wikipedia.org/wiki/Turing"
domain = url[7:23]   # "en.wikipedia.org"
```

Edit the source string, `start`, or `end` below -- the slice recomputes
live."""
    pass
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")
    # start = int(slice_start)
    # end = int(slice_end)
    # sliced = slice_source[start:end]
    # summary = f"{slice_source!r}[{start}:{end}] -> {sliced!r}"
    # return cs.md(f"```text\n{summary}\n```")


def slicing_creates_new_object():
    """## A Slice Creates a New Object

`my_str[4:7]` copies characters out of `my_str` into a completely separate string object. Once `animal` holds that copy, later reassigning `my_str` to something else has **no effect** on `animal` - they were never the same object to begin with.

Run the test: `animal` still prints `"cat"` even after `my_str` is reassigned to a sentence about a fox."""


def slicing_omitted_bounds():
    """## Omitting Start or End

Either side of `[start:end]` can be left out. Leaving out `start`
means "from the beginning"; leaving out `end` means "to the very end."
An `end` can also be negative, counting backward from the end of the
string, same as a negative index.

| Syntax | Meaning |
| --- | --- |
| my_str[8:] | index 8 through the end |
| my_str[:23] | the start through index 22 |
| my_str[:-1] | everything but the last character |

Run the test against a URL and match each slice to its printed
result."""


def slicing_stride():
    """## Stride

Slice notation also accepts a third number, `my_st[start:end:stride]` - the **stride** determines how much to advance the index after each
character is read. A stride of `2` reads every other character; a
stride of `3` reads every third.

Leaving `start` and `end` empty (`numbers[::2]`) means "the whole
string, but only every 2nd character." Run the test to see all three
strides applied to the digits `0123456789`."""


def slicing_halves(usr_text):
    """## Splitting a String in Half

Slicing composes naturally with other expressions -- the slice bounds
don't have to be literal numbers, they can be any expression."""
    pass
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # midpoint = len(usr_text) // 2
    # first_half = usr_text[:midpoint]
    # last_half = usr_text[midpoint:]
    # summary = (
    #     f'"{usr_text}"\n'
    #     f"first half:  {first_half!r}\n"
    #     f"second half: {last_half!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")


def fstrings_basics():
    """MWF 10am, 11am start here
## f-strings

A **formatted string literal** (an **f-string**) lets a programmer build a string with embedded expressions that are evaluated as the program runs. Writing `f"..."` (an `f` right before the opening quote) turns on this behavior.

Each `{...}` inside the string is a **replacement field** -- a placeholder expression whose value is substituted into the final string."""


def fstrings_equals_and_braces():
    """## Debugging With `=`, and Literal Braces

Adding `=` right before the closing `}` of a replacement field prints *both* the expression's text and its value -- handy for quick debugging without writing a separate `print(f"expr: {expr}")`.

```python
print(f"{2**2=}")   # "2**2=4"
```

Because `{` and `}` are special inside an f-string, printing a literal
brace character requires **doubling** it: `{{` becomes `{` and `}}`
becomes `}`. Run the test to see the self-documenting `=` and the
doubled-brace escapes side by side."""


def string_methods_replace():
    """## The `replace()` Method

`my_str.replace(old, new)` returns a **copy** of `my_str` with every occurrence of `old` swapped for `new` -- the original string is untouched, since strings are immutable.

An optional third argument, `my_str.replace(old, new, count)`, limits the swap to just the first `count` occurrences."""


def string_methods_find():
    """## Searching: `find()`, `rfind()`, `count()`

`my_str.find(x)` returns the index of the *first* occurrence of `x` in the string, or `-1` if `x` never appears. `find(x, start)` begins the search at `start`; `find(x, start, end)` also stops the search before `end`.

`my_str.rfind(x)` works the same way but searches **in reverse**, finding the last occurrence's index. `my_str.count(x)` returns how many times `x` occurs."""


def string_comparisons(compare_left, compare_right):
    """## Comparing Strings

`==` checks whether two strings are exactly identical. `<` and `>`
compare strings using each character's **ASCII value**, left to right
-- the first pair of characters that differs decides the result. `in`
checks whether one string is a substring of another, and is
case-sensitive: `"seph" in "Joseph"` is `True`, but `"jo" in "Joseph"`
is `False`.

Edit the two strings below to see how they compare."""
    pass
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # equal = compare_left == compare_right
    # greater = compare_left > compare_right
    # contains = compare_right in compare_left
    # summary = (
    #     f"{compare_left!r} == {compare_right!r}  ->  {equal}\n"
    #     f"{compare_left!r} >  {compare_right!r}  ->  {greater}\n"
    #     f"{compare_right!r} in {compare_left!r}  ->  {contains}"
    # )
    # return cs.md(f"```text\n{summary}\n```")


def string_is_methods():
    """## Checking String Contents: the `isX()` Methods

| Method | Returns `True` when... |
| --- | --- |
| isalnum() | every character is a letter or digit (0-9) |
| isdigit() | every character is a digit (0-9) |
| islower() | every cased character is lowercase |
| isupper() | every cased character is uppercase |
| isspace() | every character is whitespace |

These are commonly used to validate input before converting it, e.g.
checking `user_input.isdigit()` before calling `int(user_input)`. Run
the test across several sample strings and compare the results."""


def string_case_methods(case_text):
    """## Reshaping a String: `startswith()`, `endswith()`, Case, and `strip()`

More string methods, all returning a **new** string (or a `bool`)
rather than modifying the original:

| Method | Effect |
| --- | --- |
| `startswith(x)` / `endswith(x)` | `True` if the string starts/ends with `x` |
| `capitalize()` | first character uppercase, the rest lowercase |
| `lower()` / `upper()` | every character lowercased / uppercased |
| `strip()` | leading and trailing whitespace removed |
| `title()` | first letter of each word capitalized |

Edit the text box below -- every transformation is applied live to
whatever you type."""
    pass
    # summary = (
    #     f"original:    {case_text!r}\n"
    #     f"startswith('  He'): {case_text.startswith('  He')}\n"
    #     f"endswith('!  '):    {case_text.endswith('!  ')}\n"
    #     f"capitalize(): {case_text.capitalize()!r}\n"
    #     f"lower():      {case_text.lower()!r}\n"
    #     f"upper():      {case_text.upper()!r}\n"
    #     f"strip():      {case_text.strip()!r}\n"
    #     f"title():      {case_text.title()!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # summary = (
    #     f"original:    {case_text!r}\n"
    #     f"startswith('  He'): {case_text.startswith('  He')}\n"
    #     f"endswith('!  '):    {case_text.endswith('!  ')}\n"
    #     f"capitalize(): {case_text.capitalize()!r}\n"
    #     f"lower():      {case_text.lower()!r}\n"
    #     f"upper():      {case_text.upper()!r}\n"
    #     f"strip():      {case_text.strip()!r}\n"
    #     f"title():      {case_text.title()!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # summary = (
    #     f"original:    {case_text!r}\n"
    #     f"startswith('  He'): {case_text.startswith('  He')}\n"
    #     f"endswith('!  '):    {case_text.endswith('!  ')}\n"
    #     f"capitalize(): {case_text.capitalize()!r}\n"
    #     f"lower():      {case_text.lower()!r}\n"
    #     f"upper():      {case_text.upper()!r}\n"
    #     f"strip():      {case_text.strip()!r}\n"
    #     f"title():      {case_text.title()!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
    # summary = (
    #     f"original:    {case_text!r}\n"
    #     f"startswith('  He'): {case_text.startswith('  He')}\n"
    #     f"endswith('!  '):    {case_text.endswith('!  ')}\n"
    #     f"capitalize(): {case_text.capitalize()!r}\n"
    #     f"lower():      {case_text.lower()!r}\n"
    #     f"upper():      {case_text.upper()!r}\n"
    #     f"strip():      {case_text.strip()!r}\n"
    #     f"title():      {case_text.title()!r}"
    # )
    # return cs.md(f"```text\n{summary}\n```")
